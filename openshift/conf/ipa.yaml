# For LDAP freeIPA Authentication.
IPA:
  HOSTNAME: env-idm-01.infra.sat.rdu2.redhat.com
  USERNAME: uid=foreman,cn=users,cn=accounts,dc=infra,dc=sat,dc=rdu2,dc=redhat,dc=com
  PASSWORD:
  BASEDN: cn=users,cn=accounts,dc=infra,dc=sat,dc=rdu2,dc=redhat,dc=com
  GRPBASEDN: cn=groups,cn=accounts,dc=infra,dc=sat,dc=rdu2,dc=redhat,dc=com
  USER: foreman
  OTP_USER: otp_user
  TIME_BASED_SECRET:
  DISABLED_IPA_USER: disabled_user
  GROUP_USERS:
  - satadmin_01
  - satuser_01
  GROUPS:
  - satadmins
  - satusers
